<div class="Menu_logo">
    <a href="
                             {% if (app.user) %}
                             {{ path('main_home') }}
                             {% else %}
                             {{ path('app_login') }}
                             {% endif %}">
        <img src="{{ asset('images/logoSortir2.png') }}" alt="Logo Sortir.com">
    </a>
</div>