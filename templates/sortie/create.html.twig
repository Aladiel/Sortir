{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="Login_Form">
                {{ form_start(sortieForm) }}

                {{ form_widget(sortieForm) }}
                <button class="btn-success">Enregistrer</button>

                {{ form_end(sortieForm) }}
            </div>

            <div class="Login_Form">
                <p>Campus</p>
                {# {{ form_row(formRandom.campus) }} #}
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}
    Créer une sortie
{% endblock %}

