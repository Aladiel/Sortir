{% extends 'base.html.twig' %}

{% block title %}{{ parent ()}}  | S'inscrire {% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ form_start(registrationForm) }}
        {{ form_widget(registrationForm) }}
        <button type="submit" class="btn-light">Créer</button>
    {{ form_end(registrationForm) }}
{% endblock %}
